build:
  box: golang
  steps:
    - script:
        name: ls
        code: |
          ls -lahrt
    #- internal/docker-build:
     #   dockerfile: Dockerfile
      #  image-name: test
    - internal/docker-push:
        repository: $REPO
       # image-name: $IMAGE_NAME
        username: $USERNAME
        password: $PASSWORD
        tag: $TAGS, $TAGS1     
        
