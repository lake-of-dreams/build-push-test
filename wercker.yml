build:
  box: golang
  steps:
    - script:
        name: replace / with $
        code: |
          export TAG_NAME=$( echo "$WERCKER_GIT_BRANCH" | tr '/' '$')
    - internal/docker-push:
        repository: $REPO
        username: $USERNAME
        password: $PASSWORD
        tag: $TAG_NAME
