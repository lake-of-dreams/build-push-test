build:
  box: golang
  steps:
    - script:
        name: ls
        code: |
          ls -lahrt
          echo "AWS_TOKEN is $AWS_TOKEN"
          VAR=`echo -e "\ntest\n "`
          VAR_NEWLINE="$VAR"
          export VAR_NEWLINE

    #- internal/docker-build:
     #   dockerfile: Dockerfile
      #  image-name: test
    - internal/docker-push:
        repository: $REPO
        username: $USERNAME
        password: $PASSWORD
        tag: $TAGS    
        
